#include <cctype>
int main(void)
{
	int weight;
	char ch;
	int x;
	int donation;
	int quest;
	weight >= 115 && weight < 125;
	ch == 'q' || ch == 'Q';
	((x % 2) == 0) && ((x % 26) != 0);
	(donation <= 2000 && donation >= 1000) || quest == 1;
	isalpha(ch);
	return 0;
}
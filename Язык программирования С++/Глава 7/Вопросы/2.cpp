void igor(void);
float tofu(int x);
double mpg(double x, double y);
long summation(long* arr, int size);
double doctor(const char* str);
void ofcourse(struct boss list);
char* plot(struct map* list);
int main(void)
{
	return 0;
}